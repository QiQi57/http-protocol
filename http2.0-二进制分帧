
### 1.二进制分帧简介：

所有帧都共享一个8字节（64位）的首部，包括帧的
长度（16位），类型(8位)，标志(8位)，保留位(1位)，流标识符（31位）

http2.0帧的类型：
• DATA:用语传输HTTP消息体。
• HEADERS 用于传输关于流的额外的首部字段。
• PRIORITY 用于指定或重新指定引用资源的优先级。
• RST_STREAM 用于通知流的非正常终止
• SETTINGS 用于通知两端通信方式的配置数据。
• PUSH_PROMISE 用于发出创建流和服务器引用资源的要约。 （服务器推送）
• PING 用于计算往往返时间，执行“活性”检查。
• GOAWAY 用于通知对端停止在当前连接中创建流。
• WINDOW_UPDATE 用于针对个别流或个别连接实现流量控制。
• CONTINUATION 用于继续一系列首部块片段。


### 2.发起新流
客户端发送HEADERS 帧来发起新流，这个帧包含带有流ID的公用首部，可选的31位优先值，以及一组HTTP 键值对首部。
服务器通过发送PUSH_PROMISE 帧来发起推送流，这个帧与HEADERS帧等效，但它包含“要约流ID”，没有优先值。

### 3.发送应用数据
DATA帧
整个帧由公用的8字节首部，后跟HTTP净荷组成。

为减少队首阻塞，http2.0 要求DATA帧不能超过2的14次方-1（16383）字节。长度超过这个阀值，就的分帧发送










